{
  "name": "javascript-action",
  "description": "GitHub Actions JavaScript Template",
  "version": "0.0.0",
  "author": "",
  "private": true,
  "type": "module",
  "homepage": "https://github.com/actions/javascript-action#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/javascript-action.git"
  },
  "bugs": {
    "url": "https://github.com/actions/javascript-action/issues"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=26.0"
  },
  "scripts": {
    "bundle": "pnpm run format && pnpm run package",
    "ci-test": "vitest run",
    "coverage": "make-coverage-badge --output-path ./badges/coverage.svg",
    "format:check": "oxfmt --ignore-path=.oxfmtignore --check .",
    "lint": "oxlint --config=.oxlintrc.json .",
    "package": "ncc build src/index.js -o dist --license licenses.txt",
    "package:watch": "pnpm run package --watch",
    "test": "vitest run",
    "all": "pnpm run format && pnpm run lint && pnpm run test && pnpm run coverage && pnpm run package",
    "lint:fix": "oxfmt --ignore-path=.oxfmtignore . && oxlint --config=.oxlintrc.json --fix .",
    "format": "oxfmt --ignore-path=.oxfmtignore ."
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0",
    "@vitest/coverage-v8": "^4.1.11",
    "make-coverage-badge": "^1.2.0",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "vitest": "^4.1.11"
  },
  "packageManager": "pnpm@11.24.0"
}
